# Qaarib presentation runtime v2

Stable app-only presentation view for the final demo if the live backend or venue APIs are unstable.

Run:
```bash
cd demo_fallback/v2
python run.py
```

Use this honestly as a workflow showcase / presentation runtime. The live backend remains in `backend/`.
